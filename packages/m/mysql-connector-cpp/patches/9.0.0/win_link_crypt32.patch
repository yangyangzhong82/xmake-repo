--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -415,6 +415,10 @@
   endif()
 endif()
 
+if(WIN32)
+  target_link_libraries(connector INTERFACE crypt32)
+endif()
+
 #
 # Embed rpath information in the connector library.
 #
